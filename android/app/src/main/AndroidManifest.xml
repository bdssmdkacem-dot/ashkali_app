<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:label="أشكالي"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <!--
          REQUIRED by google_mobile_ads. Without this meta-data tag, calling
          MobileAds.instance.initialize() in main.dart throws and the app
          crashes before the first frame - this is almost certainly why
          nothing appeared after install. This is Google's public TEST
          App ID (safe to ship in debug/testing builds). Replace with your
          real AdMob App ID (from the AdMob console, format
          ca-app-pub-XXXXXXXXXXXXXXXX~YYYYYYYYYY) before release.
        -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713"/>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>
    </application>
</manifest>
